{
  "name": "lwb-ios",
  "private": true,
  "scripts": {
    "cap:init": "npx cap init \"LWB\" com.lwb.bible -y",
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "npx cap sync ios",
    "cap:copy": "npx cap copy ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
